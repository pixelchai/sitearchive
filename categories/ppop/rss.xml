
	<item>
	  <title>OneStepAtATime</title>
	  <link>/categories/dotnet/ppop/OneStepAtATime/</link>
	  <author></author>
	  <pubDate>2015-12-11T06:49:48+00:00</pubDate>
	  <guid>/categories/dotnet/ppop/OneStepAtATime/</guid>
	  <description><![CDATA[
	     <p>Like I mentioned in a previous post, I’m actually going to do this project sireously now.
First of all, let’s start by taking things one step at a time. As it stands, I’m probably not going to be able to do a wide a range of coding languages that I intended to.</p>

<p>Let’s start with one basic program, such as a <code>Hello World</code> program for all of the coding languages I wan’t to try first, and then we’ll expand from there.</p>

<p>Most programmers will make a <code>Hello World</code> application. I’m not “most programmers”. So let’s come up with a “first program” of our own:</p>

<h3>The Higher or Lower Game</h3>
<p>This is going to be my “first program”. It has the same real-world use as a <code>Hello World</code> program, but it’s just slightly more interesting.</p>

<p>You’ve probably heard of or played this game before, but just in case, here’s how it works:</p>

<p>A random number is generated.
User tries to guess the number.
Computer tells user if they were too high or too low.
Keep guessing untill they get the number…</p>

<h3>Pseudocode</h3>
<p>Here’s a basic implementation of it in pseudocode:</p>

<div class="highlight"><pre><code class="language-csharp" data-lang="csharp"><span class="k">set</span> <span class="n">num</span> <span class="p">=</span> <span class="n">Rand</span><span class="p">.</span><span class="n">Next</span><span class="p">(</span><span class="m">0</span><span class="p">,</span> <span class="m">1000</span><span class="p">);</span>
<span class="k">set</span> <span class="n">done</span> <span class="p">=</span> <span class="k">false</span><span class="p">;</span>
<span class="k">do</span>
<span class="p">{</span>
<span class="n">input</span> <span class="p">=</span> <span class="n">GetInput</span><span class="p">().</span><span class="n">ToInteger</span><span class="p">();</span>
	<span class="k">if</span><span class="p">(</span><span class="n">input</span> <span class="n">Equals</span> <span class="n">num</span><span class="p">)</span>
	<span class="p">{</span>
	<span class="k">set</span> <span class="n">done</span> <span class="p">=</span> <span class="k">true</span>
	<span class="n">print</span><span class="p">(</span><span class="s">&quot;Cngrtz&quot;</span><span class="p">)</span>
	<span class="k">break</span>
	<span class="p">}</span>
	<span class="k">if</span> <span class="n">input</span> <span class="n">greater</span> <span class="n">than</span> <span class="n">num</span>
	<span class="p">{</span>
	<span class="n">print</span><span class="p">(</span><span class="s">&quot;Too high&quot;</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="k">if</span> <span class="n">input</span> <span class="n">less</span> <span class="n">than</span> <span class="n">num</span>
	<span class="p">{</span>
	<span class="n">print</span><span class="p">(</span><span class="s">&quot;Too Low&quot;</span><span class="p">)</span>
	<span class="p">}</span>
	
<span class="p">}</span>
<span class="k">while</span><span class="p">(</span><span class="n">not</span> <span class="n">done</span><span class="p">);</span></code></pre></div>

	  ]]></description>
	</item>

